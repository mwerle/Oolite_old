#! /bin/sh

#if [ -e Analysis ]
#then
#	rm -rf Analysis
#fi

xcodebuild clean -project Oolite.xcodeproj -configuration Debug -target "Build All"
scan-build -k -o Analysis xcodebuild -project Oolite.xcodeproj -configuration Debug -target "Build All" > Analysis.log
