{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                clearTextureCacheAction = id; 
                createShipAction = id; 
                createShipPanelOKAction = id; 
                dumpEntityListAction = id; 
                graphicsResetAction = id; 
                logMsgClassPanelDisableAction = id; 
                logMsgClassPanelEnableAction = id; 
                modalPanelCancelAction = id; 
                otherLogMessageClassAction = id; 
                resetAndClearAction = id; 
            }; 
            CLASS = OODebugController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                createShipPanel = NSPanel; 
                createShipPanelTextField = NSTextField; 
                logMessageClassPanel = NSPanel; 
                logMessageClassSubMenu = NSMenu; 
                logMsgClassPanelTextField = NSTextField; 
                menu = NSMenu; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}